public Integer secondLargest(List<Integer> numbers) {
    Integer num = numbers.sort();
    for(Integer i : numbers)
    {
        if(i < num)
        {
            num = i;
        }
    }
    return num;
}
